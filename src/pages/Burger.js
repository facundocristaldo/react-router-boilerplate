import { Container, Typography } from "@material-ui/core";
import React from "react";

const Burger = () => {
  return (
    <Container>
      <Typography variant="h1">Burger Page</Typography>
    </Container>
  );
};

export default Burger;
